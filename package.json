{
  "name": "dialog-component",
  "description": "Dialog component",
  "version": "0.0.1",
  "dependencies": {
    "emitter-component": "*"
  }
  "component": {
    "scripts": {
      "dialog": "dialog.js"
    },
    "styles": ["dialog.css"],
    "templates": ["dialog.html"]
  }
}
