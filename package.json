{
  "name": "dialog-component",
  "description": "Dialog component",
  "version": "0.0.1",
  "dependencies": {
    "emitter-component": "*",
    "zepto": "*"
  },
  "component": {
    "styles": ["dialog.css"],
    "scripts": {
      "dialog": "dialog.js"
    },
    "templates": {
      "dialog": "dialog.html"
    }
  }
}
